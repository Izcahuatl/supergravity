{
  "libraries": [
    {
      "id": "five-three",
      "name": "Five Three",
      "description": "Lua 5.3 compatibility shims.",
      "filename": "five-three.lua",
      "doc": "<p>Adds <code>table.move</code>, <code>math.tointeger</code>, <code>math.type</code>, <code>math.ult</code>, and the <code>utf8</code> library.</p><pre><code>require('five-three')\nprint(utf8.len('hello'))</code></pre>"
    },
    {
      "id": "mini-pat",
      "name": "miniPat",
      "description": "A tiny player patting system.",
      "filename": "miniPat.lua",
      "doc": "<p>Crouch + right-click another player to pat them. Register a callback to react when someone pats you:</p><pre><code>require('miniPat')\n\nonPat('headpat', function(playerUUID, time)\n  -- your reaction here\nend)</code></pre>"
    }
  ]
}
